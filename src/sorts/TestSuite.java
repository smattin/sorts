package sorts;

import org.junit.runners.Suite;
import org.junit.runner.RunWith;

@RunWith(Suite.class)
@Suite.SuiteClasses({TestSwaps.class}) //, TestAnna.class})
public class TestSuite {
    //nothing this is pretty brain-dead requirement for JUnit4 runner
}
